
myParse := procedure(s) {
    mTokenList := myScanFormula(s);
    mIndex     := 1;
    result     := parseFormula();
    return normalize(result);
};


var mTokenList; // list of all tokens 
var mIndex;     // index to the next token

// Diese Prozedur parst eine aussagenlogische Formel gem